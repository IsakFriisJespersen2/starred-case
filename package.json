{
  "name": "starred-case",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "client:install": "cd client && npm install",
    "client:dev": "cd client && npm run dev",
    "server:install": "npm install",
    "server:start": "node --env-file=.env ./backend/bin/www",
    "server:dev": "node --env-file=.env --watch ./backend/bin/www",
    "db:reset": "node ./db/seed.js"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "^4.18.2",
    "express-basic-auth": "^1.2.1",
    "http-errors": "~1.6.3",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "fs": "^0.0.1-security"
  }
}
